{
    "type": "module",
    "outDir": "dest",
    "devDependencies": {
        "@babel/core": "^7.21.4",
        "@babel/plugin-transform-modules-commonjs": "^7.21.2",
        "@babel/types": "^7.21.4",
        "@jest/globals": "^29.5.0",
        "@types/jest": "^29.5.1",
        "@typescript-eslint/eslint-plugin": "^5.59.2",
        "@typescript-eslint/parser": "^5.59.2",
        "eslint": "^8.39.0",
        "jest": "^29.5.0",
        "pretty-format": "^27.0.2",
        "tsc-alias": "^1.8.5"
    },
    "scripts": {
        "test": "npm run build && jest --silent=false",
        "test-coverage": "npm run build && jest --silent=false --collect-coverage",
        "build": "tsc && npm run build:copy"
        "build:copy": "(cd public_html; tar cf - .) | (cd dest; tar xf -)"
    },
    "babel": {
        "env": {
            "test": {
                "plugins": [
                    "@babel/plugin-transform-modules-commonjs"
                ]
            }
        }
    }
}
